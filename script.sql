CREATE TABLE `ifcarona`.`new_table` (
  `codigo` INT NOT NULL AUTO_INCREMENT,
  `nome` VARCHAR(45) NULL,
  `modelo` VARCHAR(45) NULL,
  `valor` DECIMAL(55) NULL,
  `garantia` INT NULL,
  PRIMARY KEY (`codigo`));
